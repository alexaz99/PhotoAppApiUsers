# Required to get properties not from a local application.properties file
# but from the running Spring Cloud Config Server
spring.cloud.config.uri=http//localhost:8012
spring.cloud.config.name=PhotoAppAPIConfigServer
